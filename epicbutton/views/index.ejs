<html>
<head>
 <title>Epic Game</title>
 <style type="text/css">
 .alvaro{
 	background-color: purple;
 	font-family: cursive;
 	width: 300px;
 	}
 	</style>
 <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script type ="text/javascript" src="http://localhost:8000/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
	    $(document).ready(function (){
	    	var count = 0;
	    	var socket = io.connect();
	    	$('#count').click(function(){
	    	count ++;   
	    	socket.emit("push_button", count);	
	    	return false;    
	   		 })    	
	    	socket.on('server_response', function (count){
	    		$(".alvaro").html("The button has been pushed: "+ count + " time(s)");
	    		});    	
	    $('#reset').click(function(){
	    	count ++;   
	    	socket.emit("push_button", reset);	
	    	return false;    
	   		 })
	    socket.on('server_response', function (reset){
	    		$(".riya").html("Your count has been reset");
	    		});
	    });
    </script>
</head>
<body>
	 <div class='alvaro'></div>
	 <div class='riya'></div>
	<div id = "sum">
		<form action='/users' method='post'>
		<h4>Push the button to update the count</h4>
		<button id = "count" button name ="subject" type="submit" value= "Push the Epic Button">Push the Epic Button</button>
		<button id = "reset" button name ="subject" type="submit" value= "Reset">Reset</button>
		</form>

	 
	
</body>
</html>